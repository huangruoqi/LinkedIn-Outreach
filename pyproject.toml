[project]
name = "linkedin-outreach"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "certifi",
    "ipython>=8.39.0",
    "mcp[cli]>=1.0.0",
    "playwright>=1.40.0",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pytest-asyncio",
]
